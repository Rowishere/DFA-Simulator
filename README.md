# DFA-Simulator
A Python-based Deterministic Finite Automaton (DFA) simulator with a graphical user interface.   This project allows users to define DFA components and test input strings interactively without using the terminal.
